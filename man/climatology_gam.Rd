% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/defaults.R
\name{climatology_gam}
\alias{climatology_gam}
\title{Seasonal climatology using a GAM with cyclic splines}
\usage{
climatology_gam(
  y,
  dates,
  period = 365,
  k = 20,
  transform = scales::transform_identity(),
  newdates = NULL
)
}
\arguments{
\item{y}{numeric vector of observed values (training data)}

\item{dates}{Date vector, same length as \code{y}}

\item{period}{numeric, cycle length in days. Default 365 for annual
seasonality. Use 182.5 for semi-annual, etc.}

\item{k}{integer, basis dimension for the cyclic spline (default 20).
Controls maximum wiggliness; actual smoothness is selected by REML.}

\item{transform}{a scales transform object with \verb{$transform} and
\verb{$inverse} methods (default \code{\link[scales:transform_identity]{scales::transform_identity()}})}

\item{newdates}{Date vector of dates to predict at, or \code{NULL} to
return fitted values at the training dates (default \code{NULL})}
}
\value{
numeric vector of fitted/predicted seasonal values. Length
equals \code{length(y)} if \code{newdates} is \code{NULL}, otherwise \code{length(newdates)}.
}
\description{
Fits a smooth seasonal curve using a cyclic cubic regression spline
(\code{bs = "cc"} in \code{\link[mgcv:gam]{mgcv::gam()}}), with smoothness selected by REML.
The cyclic basis ensures continuity at the season boundary (the fitted
curve and its first two derivatives match at the cycle endpoints).
}
\details{
Uses day-of-year as the cyclic variable (scaled to [0, 1)) to handle
week 53 and leap years naturally. For non-annual periodicities, uses
the modulus of the date's numeric value with the specified period.
}
