% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/defaults.R
\name{return_climatology_gam_preds}
\alias{return_climatology_gam_preds}
\title{GAM climatology wrapper for use as \code{seas_fn} in \code{\link[=run_analogue_simulation]{run_analogue_simulation()}}}
\usage{
return_climatology_gam_preds(
  y,
  h,
  k = NULL,
  dates,
  period = 365,
  k_basis = 20,
  transform = scales::transform_identity(),
  ...
)
}
\arguments{
\item{y}{numeric vector, pre-season training data (subset of full series)}

\item{h}{integer, number of steps from end of training data to target week}

\item{k}{integer, ignored (present for interface compatibility)}

\item{dates}{Date vector for the \strong{full} time series (not just training).
Passed via \code{seas_params}.}

\item{period}{numeric, cycle length in days (default 365)}

\item{k_basis}{integer, basis dimension for the cyclic spline (default 20)}

\item{transform}{a scales transform object (default \code{\link[scales:transform_identity]{scales::transform_identity()}})}

\item{...}{ignored}
}
\value{
A list with:
\describe{
\item{pred}{numeric(1), predicted value at the target date}
\item{analogue_indices}{data.frame with one row (NA placeholder,
since GAM does not use analogues)}
}
}
\description{
Drop-in replacement for \code{\link[=return_analogue_preds]{return_analogue_preds()}} with \code{method = "seasonal"}.
Fits a cyclic GAM on the pre-season training data and predicts at the
target date (determined by \code{h} steps ahead of the training window end).
}
\details{
\strong{Usage with \code{run_analogue_simulation()}:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{run_analogue_simulation(
  ...,
  seas_fn     = return_climatology_gam_preds,
  seas_params = list(dates = data$date, period = 365, k_basis = 20,
                     transform = scales::transform_modulus(p = 1/100)),
  k_val_seas  = NULL,
  ...
)
}\if{html}{\out{</div>}}

The full \code{dates} vector for the entire time series must be passed via
\code{seas_params}. The function subsets it to match \code{y} (training window)
and uses \code{dates[length(y) + h]} as the target date.
}
