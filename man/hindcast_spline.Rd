% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/defaults.R
\name{hindcast_spline}
\alias{hindcast_spline}
\title{Hindcast using smoothing splines}
\usage{
hindcast_spline(y, spar = NULL, transform = scales::transform_identity())
}
\arguments{
\item{y}{numeric vector, the observed time series}

\item{spar}{numeric smoothing parameter, or \code{NULL} for GCV selection
(default \code{NULL}). Higher values produce smoother fits; typical range
is 0 to 1.}

\item{transform}{a scales transform object with \verb{$transform} and \verb{$inverse}
methods (default \code{\link[scales:transform_identity]{scales::transform_identity()}})}
}
\value{
numeric vector of fitted values, same length as \code{y}
}
\description{
Fits a smoothing spline to the time series with smoothness selected by
generalized cross-validation (GCV) or a user-specified \code{spar} value.
}
\details{
An optional \code{transform} argument accepts a scales transform object
(e.g., \code{\link[scales:transform_sqrt]{scales::transform_sqrt()}}, \code{\link[scales:transform_boxcox]{scales::transform_modulus()}}).
The transform is applied before fitting and inverted before returning
fitted values.
}
